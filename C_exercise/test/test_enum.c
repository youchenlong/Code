#include<stdio.h>

enum sex{male, female};
enum month{Jan, Feb, Mar, Apr, May, June, July, Aug, Sep, Oct, Nov, Dec};

int main(){
    printf("%d\n", male);
    printf("%d\n", Jan);
    return 0;
}