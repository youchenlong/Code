#include"../exercises/vector.h"

int main(){
    // vector v1;
    // vector v2;
    // create(&v1, 3);
    // create(&v2, 3);
    // init_vector(&v1);
    // init_vector(&v2);
    // printf("%f\n", norm(v1));
    // vector res;
    // res = sub(v1, v2);
    // print_vector(res);
    // res = scalar_mul(v1, 2);
    // print_vector(res);
    // printf("%f\n", dot(v1, v2));


    // vector v[3];
    // for(int i = 0; i < 3; i++){
    //     create(&v[i], 3);
    //     init_vector(&v[i]);
    // }
    // vector *res;
    // res = Schmidt_orthogonalization(v, 3);
    // for(int i = 0; i < 3; i++){
    //     printf("vector %d:", i);
    //     print_vector(res[i]);
    // }
    return 0;
}